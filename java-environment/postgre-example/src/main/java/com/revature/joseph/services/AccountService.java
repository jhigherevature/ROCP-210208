package com.revature.joseph.services;

public class AccountService {
//	public void transferFunds(User usr, String account_from, String account_to, int amount) {
//		// reference the AccountDAO to select from the first account:
//		
//		Account from = AccountDAO.selectAccount(usr, account_from); // query the account table for a type of account owned by a specific user
//		// SELECT FROM account table WHERE user_id = usr.getId() AND account_type = account_from;
//		Account to = AccountDAO.selectAccount(usr, account_to);
//		
//		from.setAmount(from.getAmount - amount); // reduce amount in From account by amount of transfer. NOTE: You may want to have an if-statement to check if the user has enough money in this account 
//		AccountDAO.updateAccount(from);
//		
//		to.setAmount(from.getAmount + amount); // increase amount in to account by amount of the transfer
//		AccountDAO.updateAccount(to);
//	} 
}
